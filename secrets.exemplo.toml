# Cole isto em Settings > Secrets do seu app no share.streamlit.io
# (não suba este arquivo pro GitHub — é só um modelo)
#
# Metabase — use METABASE_API_KEY OU (METABASE_USER + METABASE_PASSWORD).
METABASE_URL = "https://metabase.ferramentademarketing.com.br"
METABASE_API_KEY = ""
METABASE_USER = "seu_usuario_do_metabase"
METABASE_PASSWORD = "sua_senha_do_metabase"

# IA (Validação de foco e filtro de foco do Volume perdido) — configure ao
# menos uma. Cada uma serve de reserva das outras.
CEREBRAS_API_KEY = ""
GROQ_API_KEY = ""
SAMBANOVA_API_KEY = ""
GEMINI_API_KEY = ""
